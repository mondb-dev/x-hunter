# X (Twitter) credentials
X_USERNAME=
X_PASSWORD=

# OpenClaw browser profile name
OPENCLAW_PROFILE=x-learner

# GitHub — for agent auto-commit + push
GITHUB_TOKEN=
GITHUB_REPO=your-username/hunter
GIT_USER_NAME=x-learner-agent
GIT_USER_EMAIL=agent@x-learner.local

# Pump.fun live stream key (from token page on pump.fun)
PUMPFUN_STREAM_KEY=
